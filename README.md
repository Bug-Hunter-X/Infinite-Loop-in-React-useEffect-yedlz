# React useEffect Infinite Loop Bug

This repository demonstrates a common bug in React applications: an infinite loop caused by modifying state within a useEffect hook without proper dependency handling. The bug occurs in `bug.js` and its solution is provided in `bugSolution.js`.